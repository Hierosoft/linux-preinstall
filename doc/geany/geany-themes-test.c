#include <stdlib.h>
#include <stdio.h>

#define MESSAGE "Hello World"

/* Prints a message to standard output */
void print_message(const char *msg) {
	printf("%s\n", msg);
}

int main(int argc, char *argv[]) {
	print_message(MESSAGE);
	return 0;
}
















/* This code is only for testing the geany color scheme by opening this
file in Geany and matches the sample code at
https://github.com/codebrainz/geany-themes/tree/master/screenshots#readme
for the purpose of visual comparison with the themes there */
